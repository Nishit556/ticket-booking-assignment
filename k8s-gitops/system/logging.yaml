apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-stack-notes
  namespace: monitoring
  annotations:
    note: "Actual Loki deployment is managed by loki-app.yaml (ArgoCD Application)."
data:
  README: |
    This placeholder keeps the system directory intact.
    Loki & Promtail are deployed via loki-app.yaml (Helm chart).

