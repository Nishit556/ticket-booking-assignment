apiVersion: v1
kind: ConfigMap
metadata:
  name: monitoring-stack-notes
  namespace: monitoring
  annotations:
    note: "This placeholder prevents Git from deleting the directory; resources are managed via monitor-app.yaml."
data:
  README: |
    kube-prometheus-stack is installed directly by monitor-app.yaml (ArgoCD Application).
    This ConfigMap exists only to keep the directory in Git so ServiceMonitors can live alongside it.